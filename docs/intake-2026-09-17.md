# Intake — 2026-09-17 (lane D)

Eleven owner-shared sources ingested into `public/assets/skills-lab/source-catalog.json`
as rows 51–62. Every new row ships `demo.status: "absent"`; no existing row was altered
(rows 1–50 verified deep-equal to a pre-write backup after the append).

| ID | Title | Status | Licence finding | Verdict |
|----|-------|--------|-----------------|---------|
| 51 | Three.js sailing game — boat, open water, weather state machine (zackontopx) | comparator | none offered (no source) | Frame evidence supports a coordinated weather state machine (cloud volume, water, rain, sun colour, exposure) as the transferable object; no code exists to extract. |
| 52 | Tripo characters + Mixamo rigs + agent-built island village of NPCs (DilumSanjaya) | comparator | none offered (no source) | Frames verify the artefact (23 named NPCs with roles/activities, quest economy); the Tripo/Mixamo/Astra pipeline split is the poster's claim only. |
| 53 | verdant-forest — instanced LOD woodland (Leonxlnx) | method-extracted | NO LICENCE FILE — all five candidate names 404 at pin 4252ffd5 | Learn-only. `app/forest/vegetation.ts` read at pin; method restated in our own words (per-cell instanced LOD swap, distance-budgeted density culling). |
| 54 | DeepSeek V4.1 Flash authoring a pickup truck in Blender (victormustar) | comparator | model weights MIT (Hugging Face repo statement); no source for the video | Model existence/release (2026-09-10) checked separately from the poster's "mechanically accurate" claim; frames verify a Blender 5.2 scene with modelled underbody, not authorship. |
| 55 | Project Aether — claimed self-hosted source release (verrial_) | comparator | unknown — repository not located | Release UNVERIFIED: the post's only outbound link resolves to a Discord invite; GitHub search found nothing. Blocker recorded with unblock action. |
| 56 | Tripo3D smart-mesh game-ready assets — Assassin Mage / Hexen (majidmanzarpour) | comparator | none offered (hosted vendor service) | Comparator only; quoted post's media was not captured. Same author as row 27, different artefact — no merge. |
| 57 | CUDA-kernel-to-WGSL transpiler + JONSWAP two-scale ocean (SamG-Coder) | method-extracted | MIT, both repos — licence FILES read at pins 9222d16 / c272bb7 | Strongest row of the wave: `kernels/ocean.cu` and `docs/research.md` read at pins; method restated. Note the r186 three pin vs our 0.185.1. |
| 58 | DERMIS — CUDA-authored WebGPU procedural human renderer (SamG-Coder) | comparator | MakeHuman base claimed CC0 — UNVERIFIED (licence page 404, README silent) | Explicitly NOT a likeness/face-capture route; recorded as such. No DERMIS repository located. |
| 59 | Graalitoo X article | comparator | unknown — body not retrievable | Syndication does not serve articles; Jina reader 403; direct fetch returned an empty client shell. Contents deliberately NOT inferred from rows 1–3. |
| 60 | Tesana — hosted AI game maker with Image to Game (x.com/tesanaai) | comparator | proprietary hosted product | Identity resolved from its own site: image-to-playable-game feature announced 2026-09-12. Vendor claims untested. |
| 61 | COD 4 LiveLink in UE5 with multiplayer (thatkidpolito) | comparator | none offered (unreleased) | Frames verify the editor state (BogMap, VesperLiveLink actor, Pixel Streaming). CoD content recorded as Activision IP, private-use only, never redistributed. |
| 62 | AGRPLUGIN — AGR-based UE importer work-in-progress (mallowed) | comparator | none offered (unreleased) | Frames verify the importer session in detail (per-entity Sequencer tracks, export-asset trees, CoD Shader tab). Same private-use restriction recorded. |

## What I could not establish

- **Row 55 (Project Aether):** the repository URL. The post's "find it here" link resolves to a
  Discord invite, not a repo; GitHub search returned nothing. Joining the Discord is an external
  action needing owner approval — blocker and unblock action are recorded on the row.
- **Row 59 (Graalitoo article):** the article body. All three retrieval routes failed
  (syndication, Jina reader 403, direct fetch returns an empty shell). Recorded as comparator
  with the evidence note per the brief; contents were not guessed.
- **Row 58 (DERMIS):** MakeHuman's base-mesh licence. The poster claims CC0; MakeHuman's own
  licence page 404s and the repository README is silent. The claim stays UNVERIFIED on the row.
- **Row 53 (verdant-forest):** whether the GitHub owner `Leonxlnx` is the same person as
  catalogue row 44's thin comparator `x.com/lexnlin`. Plausible handle resemblance only;
  recorded as UNVERIFIED.
- **Row 62:** what "AGR" abbreviates. Not derivable from the frames; recorded as unestablished.
- **Rows 61/62:** whether any captured animation originates from the retail games or is
  re-performed. Not derivable from frames; recorded as unestablished with the IP limitation.
- **Rows 51/52/54:** which tools actually produced what. Pipeline attributions (including
  "DeepSeek V4.1 Flash built this") are poster claims; only the artefacts themselves are
  frame-verified.

## Verification statement

Verified this lane: catalogue parses; `node -e` count = 62 from the repo root; ids 1–62
sequential; rows 1–50 deep-equal to the pre-write backup; `updatedAt` = 2026-09-17 (wave 3);
adapterNotes extended by pure append. Licence findings for rows 53/57 come from licence FILES
fetched at commit pins (five-candidate probe where none exists), not from API fields.

Not verified: no lab demo was built for any row (all `demo.status: absent` by design); no GPU
or browser run of north-swell/cuda-webshader; no pixel-level validation of anything; MakeHuman
licence (above). Frame observations cited for rows 51, 58, 61, 62 were read by the orchestrator
and are treated as verified per the brief; frames for rows 52 and 54 were read by this lane.

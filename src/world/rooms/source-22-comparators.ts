/**
 * Source 22 — Environment-art comparators, 2026-08-24 batch.
 *
 * An honest empty room, by catalogue instruction. The row is a comparator with
 * a recorded blocker: no repository, no published technique and no licence for
 * any of the three links, so there is no method to demonstrate. The register
 * explicitly refuses the substitution of building original scenes under this
 * title, so this door stays a stub and says why.
 */
import type { RoomDefinition } from '../contract';

export const room: RoomDefinition = {
  sourceId: 22,
  skill: 'unmapped',
  title: 'Environment-art comparators, 2026-08-24 batch',
  summary:
    'Three linked pages held as quality targets only: dense ground cover, '
    + 'heavy wheel-ground feel and readable multi-target lock-on.',
  kind: 'stub',
  limitation:
    'Blocked: revo-realms serves no meaningful HTML and both game links return '
    + 'storefront furniture only, so no method was recoverable and nothing is '
    + 'demonstrated. The register forbids substituting original scenes under '
    + 'this title.',
};

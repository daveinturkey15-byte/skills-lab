/**
 * Room for source 44 — Thin comparators from the 2026-08-31 sweep.
 *
 * Honest stub, by catalogue design. Neither post resolves to a repository, a
 * released tool or a licence, so there is no method to stage — that absence is
 * the finding. Any technique they suggest gets built originally under our own
 * title, never under this door.
 */
import type { RoomDefinition } from '../contract';

export const room: RoomDefinition = {
  sourceId: 44,
  skill: 'unmapped',
  title: 'Sweep comparators (no method)',
  summary: 'Two one-line visual comparators with no technique behind them; this room stays empty rather than inventing one.',
  kind: 'stub',
  limitation:
    'Comparator only: no repository, tool or licence was ever published, so nothing is demonstrated and nothing is owed.',
};

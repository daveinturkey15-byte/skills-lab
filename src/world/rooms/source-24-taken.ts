import type { RoomDefinition } from '../contract';

/**
 * Source 24 — TAKEN: browser survival horror, dense ground cover and night sky.
 *
 * Stub by honesty, not by omission. The playable page was fetched but no
 * source repository resolves from it, the register closed the search negative,
 * and this lane did not reopen it — so there is no method to stage. The
 * catalogue's own alternate (our own ground cover under our own title) would
 * credit this door with work that is not this source's, which the blocker
 * forbids. An empty room with the reason on the wall is the correct exhibit.
 */
export const room: RoomDefinition = {
  sourceId: 24,
  skill: 'unmapped',
  title: 'TAKEN — dense cover and night sky',
  summary:
    'TAKEN is a browser survival horror whose repository was never located, so there is no method to demonstrate.',
  kind: 'stub',
  limitation:
    'BLOCKED: no source exists to implement — the playable page carries no linked repository and the register search closed negative. Dense low ground cover plus our own night-sky work is the recorded alternate, under our own title and never credited to this source.',
};

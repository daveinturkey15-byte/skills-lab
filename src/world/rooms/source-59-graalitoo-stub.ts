/**
 * Source 59 — Graalitoo X article, body not retrievable (honest stub).
 *
 * The article contents are UNKNOWN: the fetch returned no body, there is no
 * repository and no pinned technique, and the register forbids inferring this
 * article from the author's other rows. There is nothing demonstrable, so the
 * room is a stub — a door that opens onto an honest empty room, which the
 * contract exists for.
 */
import type { RoomDefinition } from '../contract';

export const room: RoomDefinition = {
  sourceId: 59,
  skill: 'unmapped',
  title: 'Graalitoo X article — not retrieved',
  summary: 'The article body was never retrievable, so there is nothing to demonstrate yet.',
  kind: 'stub',
  limitation:
    'Article contents UNKNOWN — do not infer them from the author\u2019s other rows. No method, no demo, no verification of any kind.',
};
